// Google Calendar integration
// The Google Calendar widget now uses per-user iframe embed (client-side only).
// Each user connects their own Google Calendar directly in the widget.
// No server-side API is needed for this approach.
